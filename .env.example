# Brevo SMTP Configuration
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USERNAME=your-brevo-login-email@example.com
SMTP_PASSWORD=your-brevo-smtp-key-here
SMTP_FROM_EMAIL=your-sender-email@example.com
SMTP_FROM_NAME=BPO Acceptor

# Recipient Email (where leads will be sent)

# Webhook Security (optional - for webhook signature verification)
WEBHOOK_SECRET=your-webhook-secret-token-here

# Application Settings
APP_NAME=BPO Acceptor Lead Service
DEBUG=False
